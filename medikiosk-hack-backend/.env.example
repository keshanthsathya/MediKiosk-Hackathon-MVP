# Server
PORT=3000
NODE_ENV=development

# Supabase (Project Settings -> API)
SUPABASE_URL=https://nmejvqneqrrugavdvkij.supabase.co
SUPABASE_SERVICE_KEY=sb_publishable_rQE7vSs4hJdu9BvpSaAb3g_wEGVL-tp

# Anthropic (for clinical history structuring)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx
ANTHROPIC_MODEL=claude-sonnet-4-5

# JWT (doctor login)
JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=12h

# Email (nodemailer via SMTP relay, e.g. Gmail app password / Mailtrap / Brevo)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass
SMTP_FROM="MediKiosk <no-reply@medikiosk.app>"

# Supabase Storage bucket name for uploaded documents
SUPABASE_STORAGE_BUCKET=documents

# Base URL used to build public report links in emails
PUBLIC_BASE_URL=http://localhost:3000
